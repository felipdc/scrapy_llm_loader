from setuptools import setup, find_packages

setup(
    name="scrapy_llm_loader",
    version="0.3",
    description="A Scrapy extension for data extraction using LangChain",
    packages=find_packages(),
)
